# requirements.txt

# --- Core Rasa Libraries ---
rasa~=3.6.0
rasa-sdk~=3.6.0

# --- Dependências de Pipeline (Adicione apenas o que você usa) ---
# Se você usa componentes específicos, adicione-os aqui.
# Exemplo para spaCy (lembre-se de baixar o modelo também: python -m spacy download en_core_web_md)
spacy~=3.5.0

# Exemplo para TensorFlow (o Rasa já instala uma versão, mas você pode fixar se precisar)
tensorflow~=2.12.0

# --- Dependências de Conectores e Outros ---
# Adicione outras bibliotecas que seu projeto usa diretamente.
# Pela sua lista, talvez você precise de algumas destas:
psycopg2-binary~=2.9.5   # Se você usa PostgreSQL para o tracker
pymongo~=4.3.3          # Se você usa MongoDB
boto3~=1.34.0           # Se você usa AWS (S3, etc.)
slack_sdk~=3.20.0       # Se você usa o conector do Slack
twilio~=8.0             # Se você usa o conector do Twilio